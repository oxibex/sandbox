server:
  port: 8888
spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:///E:/project/sandbox/microservice/configuration
  application:
    name: config-server