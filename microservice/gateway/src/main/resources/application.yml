spring:
  application:
    name: gateway
  config:
    import: optional:configserver:http://localhost:8888/
  cloud:
    gateway:
      routes:
        - id: test_service
          uri: lb://TEST-SERVICE
          predicates:
            - Path=/test-service/**
          filters:
            - StripPrefix=1

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8080

